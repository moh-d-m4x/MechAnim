fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
requests>=2.31.0

# PyTorch for TAPIR tracking (development)
# Install manually with CUDA support:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# Or CPU only:
#   pip install torch torchvision

# ONNX Runtime for production distribution (downloaded separately)
